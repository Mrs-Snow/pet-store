spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/pet?serverTimezone=UTC
    username: root
    password: 12345678
mybatis-plus:
    configuration:
      #自动驼峰命名
      map-underscore-to-camel-case: false
      log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
server:
  servlet:
    context-path: /mrsnow
  port: 17777

myFilter:
    passList:
      - /login
      - /goods
      - /cart
      - /goodsDetail
      - /user
      - /store
      - /preferential
      - /address
